{
  "name": "turbo-app",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "cd packages/db && npx prisma generate && cd ../..",
    "start:backend": "cd packages/db && npx prisma migrate dev && cd ../.. && cd apps/http-backend && npm run start",
    "start:websocket": "cd packages/db && npx prisma migrate dev && cd ../.. && cd apps/ws-backend && npm run start",
    "start:frontend": "cd packages/db && npx prisma migrate dev && cd ../.. && cd apps/web && npm run start"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
